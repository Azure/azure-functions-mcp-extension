{
  "name": "workout-tracker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:start-workout && npm run build:active-workout",
    "build:start-workout": "cross-env BUILD_ENTRY=start-workout vite build",
    "build:active-workout": "cross-env BUILD_ENTRY=active-workout vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "cross-env": "^7.0.3",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
